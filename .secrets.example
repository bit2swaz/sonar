# Copy this file to .secrets before running scripts/local-ci.sh.
# File format is compatible with act --secret-file.
# Replace GITHUB_TOKEN with a real token if you want to run workflows that
# depend on GitHub-authenticated actions or GitHub API access.

GITHUB_TOKEN=ghp_dummy_local_act_token
